@model WebApplication1.Models.PosteTravail

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PosteTravail</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="NumPoste" />
            <div class="form-group">
                <label asp-for="TypePoste" class="control-label"></label>
                <select asp-for="TypePoste" class="form-control"></select>
                <span asp-validation-for="TypePoste" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmployeID" class="control-label"></label>
                <select asp-for="EmployeID" class="form-control" asp-items="ViewBag.EmployeID"></select>
                <span asp-validation-for="EmployeID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
