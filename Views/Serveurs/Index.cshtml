@model IEnumerable<WebApplication1.Models.Serveur>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.NumServeur)</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumServeur)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumServeur">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumServeur">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumServeur">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
